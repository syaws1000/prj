### ✅ 객관식 문제 15문제

1. **CASE WHEN 구문에서 "단순 CASE 구문"의 특징은 무엇인가요?**
   - A. 복잡한 조건식을 사용할 수 있다.
   - B. 특정 컬럼의 값을 다른 값과 비교하여 결과를 반환한다.
   - C. 조건을 평가하여 값을 반환하지 않는다.
   - D. `ELSE` 부분이 필수이다.

2. **"검색 CASE 구문"에서 사용되는 특징은 무엇인가요?**
   - A. 단일 값만 비교할 수 있다.
   - B. 복잡한 조건식을 사용할 수 있다.
   - C. `WHEN` 절에 반드시 `IS NULL` 조건을 포함해야 한다.
   - D. `ELSE` 부분이 필요하지 않다.

3. **다음 중 `INSTR()` 함수의 주요 용도는 무엇인가요?**
   - A. 문자열의 길이를 계산한다.
   - B. 문자열에서 특정 문자의 위치를 찾는다.
   - C. 문자열을 자른다.
   - D. 문자열을 특정 길이로 채운다.

4. **`INSTR()` 함수에서 문자열을 찾을 때 반환되는 값은 무엇인가요?**
   - A. 문자열의 길이
   - B. 해당 문자의 위치(1부터 시작)
   - C. 문자열의 첫 번째 문자
   - D. 문자열의 마지막 문자

5. **다음 중 `LPAD()` 함수의 사용 목적은 무엇인가요?**
   - A. 문자열의 왼쪽 끝에 지정된 문자로 채운다.
   - B. 문자열의 오른쪽 끝에 지정된 문자로 채운다.
   - C. 문자열의 길이를 자른다.
   - D. 문자열에서 특정 문자를 삭제한다.

6. **`LTRIM()` 함수의 기능은 무엇인가요?**
   - A. 문자열의 오른쪽 끝에서 특정 문자를 제거한다.
   - B. 문자열의 왼쪽 끝에서 특정 문자를 제거한다.
   - C. 문자열의 양쪽 끝에서 특정 문자를 제거한다.
   - D. 문자열을 특정 길이로 자른다.

7. **`TRIM()` 함수에서 양쪽 끝에서 제거할 문자를 지정하는 방식은 어떤 방식인가요?**
   - A. `TRIM(컬럼 FROM 문자)`
   - B. `TRIM(문자 FROM 컬럼)`
   - C. `TRIM(문자, 컬럼)`
   - D. `TRIM(컬럼, 문자)`

8. **`SYSDATE` 함수가 반환하는 데이터 타입은 무엇인가요?**
   - A. NUMBER
   - B. VARCHAR
   - C. DATE
   - D. TIMESTAMP

9. **`MONTHS_BETWEEN()` 함수는 어떤 값을 반환하나요?**
   - A. 두 날짜 사이의 연도 차이
   - B. 두 날짜 사이의 월 차이
   - C. 두 날짜 사이의 일 수
   - D. 두 날짜 사이의 시간 차이

10. **`ADD_MONTHS()` 함수에서 사용하는 두 번째 인자에 대해 설명하세요.**
    - A. 날짜에 더할 일 수
    - B. 날짜에 더할 분 수
    - C. 날짜에 더할 월 수
    - D. 날짜에 더할 시간 수

11. **자바에서 1:1 관계를 설정할 때, 클래스 안에 다른 클래스 타입의 필드를 선언하는 이유는 무엇인가요?**
    - A. 객체 간의 연결을 만들기 위해
    - B. 객체 간의 상속 관계를 설정하기 위해
    - C. 객체의 복잡도를 줄이기 위해
    - D. 객체의 메서드를 재사용하기 위해

12. **자바에서 "포함 관계(Has-A 관계)"란 무엇을 의미하나요?**
    - A. 한 객체가 다른 객체의 기능을 상속받는 관계
    - B. 한 객체가 다른 객체를 포함하고 있는 관계
    - C. 두 객체가 서로 의존적인 관계
    - D. 두 객체가 같은 메모리를 공유하는 관계

13. **자바에서 `ArrayList`와 `배열`의 차이점은 무엇인가요?**
    - A. 배열은 크기가 동적이고, `ArrayList`는 고정 크기이다.
    - B. `ArrayList`는 크기가 동적이고, 배열은 고정 크기이다.
    - C. 배열은 객체이고, `ArrayList`는 기본 데이터 타입이다.
    - D. `ArrayList`는 한 가지 데이터 타입만 저장할 수 있다.

14. **다음 중 `ArrayList`를 사용할 때의 장점은 무엇인가요?**
    - A. 메모리 공간을 절약할 수 있다.
    - B. 크기를 동적으로 조절할 수 있다.
    - C. 배열보다 빠르다.
    - D. 데이터 타입에 제약이 없다.

15. **다음 코드에서 "고양이가 산책을 갑니다" 출력 시, 어떤 클래스를 참조하나요?**
```java
Leash myLeash = new Leash("빨간색");
Dog myDog = new Dog("초코", myLeash);
myDog.goWalk();
```
    - A. `Dog` 클래스
    - B. `Leash` 클래스
    - C. `Person` 클래스
    - D. `Toy` 클래스
    
    
    
 