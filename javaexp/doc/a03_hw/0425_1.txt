### 🔷 Oracle SQL 관련 (10문제)

1. `NVL(컬럼, 대체값)` 함수에 대한 설명으로 옳은 것은?  
	컬럼과 대체값이 데이터 유형이 같아야 한다.(주의)
	nvl(empno, '25') (x)
	nvl(empno, 0) (O)  empno 컬럼에 데이터가 없을 때, 0으로 처리된다.
   A. NULL이 아닌 값을 대체값으로 변경한다  
   #B. NULL인 경우 지정한 대체값으로 반환한다  
   C. 모든 데이터 타입을 자동으로 변환한다  
   D. 반드시 숫자만 처리할 수 있다  

2. `NVL2(컬럼, 값1, 값2)` 함수는 다음 중 어떤 동작을 하나?  
	nvl2는 값1, 값2가 컬럼의 데이터 유형과 다르더라도 상관없다.
	nvl2(empno, empno, '사원정보없음') empno값 값이 있을 때, empno쓰고, 
	그외는 문자로 사원정보없을 출력
   A. 값1과 값2를 모두 더한다  
   B. 컬럼이 NULL일 경우 값1 반환  
   #C. 컬럼이 NULL이 아닐 경우 값1, NULL일 경우 값2 반환  
   D. 항상 NULL만 반환한다  

3. `NULLIF(값1, 값2)` 함수에 대한 설명 중 틀린 것은?  
   A. 값1과 값2가 같으면 NULL을 반환한다  
   B. 두 값이 다르면 값1을 반환한다  
   #C. 항상 NULL을 반환한다  
   D. 중복 데이터 제거 등에 활용할 수 있다  

4. 다음 중 `CONCAT()` 함수의 특징으로 올바른 것은?  
	CONCAT(값1, 값2), 값1||값2||값3  
	CONCAT(값1, 값2, 값3) (X)
   A. 문자열이 아닌 값을 결합할 수 없다  
   #B. 두 개의 인자만 연결할 수 있다  
   C. 여러 개의 값을 동시에 연결할 수 있다  
   D. 항상 공백을 자동 추가한다  
            123456
5. `SUBSTR('ORACLE', 2, 3)`의 결과는?  
	SUBSTR('ORACLE',-5, 3) ==> 뒤에서부터 -1 시작하여 해당 갯수를 추출
   A. ORA  
   #B. RAC  
   C. RACLE  
   D. RA  

6. `LENGTH('Oracle SQL')` 함수의 반환값은?  
   #A. 10   중간에 공백도 포함해서 글자수를 리턴한다.
   	LENGTH('안녕하세요') 한글도 5개 리턴
   B. 9  
   C. 11  
   D. 12  

7. `LENGTHB()` 함수에 대한 설명으로 옳지 않은 것은?  
   A. 문자열의 바이트 수를 반환한다  
   B. 한글은 일반적으로 3바이트로 계산된다  
   #C. 문자 수와 항상 같다  
   LENGTHB('안녕하세요') ==> 15  한글 한자당 3바이트
   D. ASCII와 멀티바이트 환경에서 차이를 보일 수 있다  

8. `UPPER(ENAME)` 함수의 기능으로 올바른 것은?  
   A. 숫자를 대문자로 바꾼다  
   #B. 영문자만 모두 대문자로 바꾼다  
   C. 특수문자도 대문자로 바꾼다  
   D. 한글도 대문자로 변환한다  

9. `SELECT NVL(COMM, 0)`의 결과가 0이 될 수 있는 조건은?  
   A. COMM이 음수일 때  
   B. COMM이 숫자일 때  
   #C. COMM이 NULL일 때  
   D. COMM이 공백일 때  

10. 다음 중 `DEPTNO = 10`일 때 `NULLIF(DEPTNO, 10)`의 결과는?  
	NULLIF(값1, 값2) : 두개의 값이 같을 때는 NULL 다를 때는 값1을 리턴 처리..
   A. 10  
   B. 0  
   #C. NULL  
   D. DEPTNO + 10  

---

### 🔷 JavaScript `prompt()` 관련 (5문제)

11. `prompt()` 함수의 주요 기능은 무엇인가?  
   A. 콘솔에 입력받는다  
   B. 숫자를 계산한다  
   #C. 사용자로부터 문자열 입력을 받는다  
   D. 함수를 실행한다  

12. 사용자가 `prompt()` 입력창에서 취소를 누르면 반환되는 값은?  
   A. 빈 문자열  
   B. undefined  
   #C. null  
   D. false  

13. `prompt("이름을 입력하세요", "홍길동")`에서 `"홍길동"`은 어떤 역할인가?  
	홍길동을 기본적으로 입력TEXT에 입력이 될게할 때, 사용
   A. 입력 유도 메시지  
   #B. 기본 입력값  
   C. 변수명  
   D. 오류 메시지  

14. prompt로 받은 값을 숫자로 바꿔 계산하려면 어떤 함수가 필요한가?  
	parsetInt(prompt("물건가격입력","0")) : 2중 함수 적용으로 입력된 문자열을 숫자로 변환 처리..
   #A. parseInt(), Number()  숫자형 문자열  "25"  "3.14" ==> 25, 3.14 
   B. String()  
   C. Boolean()  
   D. prompt()  

15. 다음 중 prompt() 함수의 특징으로 옳지 않은 것은?  
   A. 항상 문자열을 반환한다  
   B. 입력창이 뜨는 동안 코드 실행이 일시 중지된다  
   #C. HTML 내에서는 작동하지 않는다  
   D. 브라우저 환경에서만 사용된다  

