### 객관식 문제 15문제:

1. **JSP에서 세션 범위(session scope)에 저장되는 데이터는 언제까지 유효한가요?**
	기본 자바 변수 : 데이터의 크기 종류에 따라
	jsp 변수 : 변수의 유지 page에 따른 처리..
	1. page : 현재 페이지에서만 변수를 사용 가능
	2. request : 요청 범위에서 변수를 사용 가능..
		forward로 페이지 이동될 때, (request, response)가져가서 이동하는 경우를 말한다.
		request.setAttribute("key","value");
	3. session : 하나의 브라우저(클라이언트)와 하나의 서버 연결관계에 있을 때, 설정한 변수가
		이 관계가 유지될 때가 변수를 사용 가능
		브라우저가 변경되거나, 서버가 리스타트등 변경되면 사라진다.
	4. application : 해당 변수를 선언할 때, 서버가 유지되는 경우에 설정한 변수 사용 가능..
		
		


   * A. 페이지가 새로 로드될 때
   # B. 한 HTTP 요청이 끝날 때
   * C. 클라이언트가 웹 애플리케이션을 종료할 때까지
   * D. 웹 애플리케이션 서버가 재시작될 때까지

2. **다음 중 `response.sendRedirect()`를 사용한 후의 결과로 올바른 것은 무엇인가요?**
	서버에서 이동되는 것을 앞선 forward처리와 차이가, request, response를 가지고 이동되지
	않기 때문에 request범위는 사라진다. 그러나 session/application 범위에 있는 변수는 
	계속 유지된다.
   * A. 서버 내에서 다른 페이지로 이동한다.
   # B. 클라이언트의 URL이 변경되고 새로운 요청이 서버로 전송된다.
   * C. 클라이언트의 요청 데이터는 그대로 유지된다.
   * D. URL에 포함된 모든 파라미터가 자동으로 제거된다.

3. **JSP에서 `forward`를 사용할 때의 특징으로 올바른 것은 무엇인가요?**

   * A. 클라이언트의 URL이 변경된다.
   # B. 요청과 응답 객체를 그대로 유지한 채 다른 JSP 페이지로 이동한다.
   		<c:set var="msg" value="안녕하세요" scope="request"/>ㅣ
   		RequestDispatcher rd = request.getRequestDispatcher("이동할페이지");
   		rd.foward(request, response);
   		<jsp:forward page="이동할페이지"/>
   		
   		이동한페이지 : ${msg} : 해당 내용이 유지되어 표현할 수 있다..(forward로 처리할 때, 
   			화면은 이동한페이지이나, url에는 이전 페이지 이름으로 설정되어 있다)
   		
   * C. 세션 정보는 사라진다.
   * D. 요청 정보를 다른 페이지로 전달할 수 없다.

4. **다음 중 `session scope`에 저장된 값이 사라지는 시점은 언제인가요?**

   * A. 웹 애플리케이션 서버가 종료될 때
   * B. 페이지가 새로 로드될 때 : 모든 페이지 같은 브라우저에서 다 닫지 않을 때..
   # C. 세션 타임아웃이 발생할 때
   * D. HTTP 요청이 끝날 때

5. **다음 중 `jsp:useBean` 태그의 주요 기능은 무엇인가요?**

   * A. 클라이언트에서 데이터를 가져오기 위한 태그
   # B. 자바 객체를 생성하여 JSP 페이지에서 사용할 수 있게 한다.
   * C. HTML 폼 데이터를 서버로 전송하는 기능을 제공한다.
   * D. 페이지 내에서 자바스크립트 코드만을 실행할 수 있게 한다.

6. **JSP에서 `c:if` 태그를 사용하여 조건문을 처리하는 데 필요한 속성은 무엇인가요?**

   # A. `test`
   * B. `value`
   * C. `condition`
   * D. `expression`

7. **JSP에서 `c:choose`와 함께 사용하는 태그는 무엇인가요?**

   # A. `c:when`과 `c:otherwise`
   * B. `c:if`와 `c:set`
   * C. `c:loop`와 `c:end`
   * D. `c:forEach`와 `c:begin`

8. **`request.getRequestDispatcher()` 메서드를 사용한 후, 호출되는 메서드는 무엇인가요?**

   # A. `forward()` <jsp:forward page="이동할페이지명"/>
   * B. `sendRedirect()`
   * C. `include()`
   * D. `refresh()`

9. **`<jsp:setProperty>` 태그의 주된 목적은 무엇인가요?**

   # A. 자바 객체의 값을 설정하는 것 : 요청값을 처리할 때도 사용된다..
   * B. HTML 폼 데이터를 서버로 전송하는 것
   * C. 웹 페이지에 스타일을 추가하는 것
   * D. 서버에서 클라이언트로 데이터를 보내는 것

10. **세션 스코프의 특성으로 올바른 것은 무엇인가요?**

* A. 사용자가 요청한 페이지에서만 유효하다.
# B. 사용자가 웹 애플리케이션을 종료할 때까지 유지된다.
* C. 모든 사용자에게 동일한 값을 공유한다.
* D. 세션은 클라이언트의 브라우저가 종료될 때마다 사라진다.

11. **JSP에서 `c:set` 태그를 사용하는 이유는 무엇인가요?**
	<c:set var="변수명" value="할당할데이터" />
# A. 변수를 선언하고 그 값을 설정하기 위해
* B. 데이터를 클라이언트로 전송하기 위해
* C. 세션 값을 초기화하기 위해
* D. 조건문을 처리하기 위해

12. **다음 중 JSP에서 `response.sendRedirect()`를 사용하면 발생하는 결과는 무엇인가요?**

* A. 서버에서 다른 페이지로 자동 전환된다.
# B. 클라이언트가 새로 요청을 보내고 서버가 새로운 페이지를 처리한다.
* C. 기존 요청 정보가 새로운 페이지로 전달된다.
* D. 클라이언트의 URL이 변경되지 않는다.

13. **JSP에서 `response.getWriter().write()`는 무엇을 수행하는가요?**
	내장되어 선언되어 있음 :PrintWriter out = response.getWriter()
	out.print() : 그래서 객체 없이 참조변수(out)을 바로 사용할 수 있음.
	out.write()
	
	서버에서는 html코드 브라우저에 전송하는 역할,   html코드를 브라우저에서 해석해서 출력하는 역할..

* A. 클라이언트에게 HTML 페이지를 반환한다.
* B. 서버에 데이터를 저장한다.
# C. 서버에서 클라이언트로 텍스트를 전송한다.
* D. 데이터베이스에서 값을 검색한다.

14. **다음 중 `forward`와 `redirect`의 차이점으로 올바른 것은 무엇인가요?**

* A. `forward`는 URL이 변경되며, `redirect`는 URL이 변경되지 않는다.
* B. `forward`는 클라이언트 요청을 새로 시작하고, `redirect`는 서버 내에서 페이지 이동을 한다.
# C. `redirect`는 URL을 변경하고, 새 요청을 클라이언트에서 서버로 보낸다.
* D. `redirect`는 서버 내에서 페이지 이동을 하며, `forward`는 클라이언트의 URL을 변경하지 않는다.

15. **`jsp:useBean` 태그의 `scope` 속성의 기본값은 무엇인가요?**

# A. `session`
* B. `request`
* C. `page`
* D. `application`

